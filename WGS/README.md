# cHL_mutation_calling/WGS
Contains two pipelines:

1. Snakemake pipeline to call SNVs using Mutect2 in tumour-only mode and perform functional annotation (Funcotator) of variants

2. Pipeline adapated from the mCA_WGS paper to call mCAs from WGS data (https://github.com/tangdavid/mCAs_WGS)

